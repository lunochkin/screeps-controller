all:
  children:
    screeps_servers:
      hosts:
        screeps-server:
          ansible_host: 5.39.249.149
          ansible_user: root
          ansible_ssh_private_key_file: ~/.ssh/id_rsa  # Uncomment if using SSH key
          # ansible_password: YOUR_PASSWORD  # Uncomment if using password
      vars:
        # screeps_domain: your-domain.com  # Optional: if you have a domain
        screeps_port: 21025
        screeps_data_dir: /opt/screeps
        docker_compose_version: "3.8"
